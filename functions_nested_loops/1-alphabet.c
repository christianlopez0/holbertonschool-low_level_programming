#include "main.h"
/**
 * main - prints the string "alphabet in lowercase"
 * Return: 0 on success.
 */
	int main (void) 
{

	char alphabet ='a'

		;while (alphabet<= 'z')
{

	_putchar(alphabet);

	alphabet++;
	}

_putchar('\n');
return (0);
}
